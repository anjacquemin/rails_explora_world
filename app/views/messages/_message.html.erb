
<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.email.delete_suffix("@gmail.com").capitalize %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
